<div class="jumbotron">

    <% projects.forEach(project => { %>
    <div><%= project.name%></div>
    <div class="btn-row">
        <%- include('../partials/crudBtn', {url:`/projects/${project.id}`})%>
        <%- include('../partials/deleteForm', {url:`/projects/${project.id}`})%>
    </div>
    <% }) %>

    <div id="menu">
        <ul>
            <li><a href="projects/new">Adicionar projeto</a></li>
        </ul>
    </div>
</div>